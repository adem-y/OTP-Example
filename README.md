## I made a basic otp project using spring boot and twilio.
### There are two endpoints:
   ### /otp/generate
   #### takes phoneNumber and userName as parameters
   ### /otp/validate
   #### takes otp and userName as parameters
